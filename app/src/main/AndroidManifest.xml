<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.suri5.clubmngmt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Common.MainActivity"
            android:launchMode="singleTop">
        </activity>

        <activity android:name=".Common.SplashActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Person.PersonShowActivity">

        </activity>

        <activity android:name=".Person.PersonEditActivity">

        </activity>

        <activity android:name=".Person.PersonAddActivity"/>
        <activity android:name=".Schedule.ScheduleActivity"/>
        <activity android:name=".Group.addGroup_info"/>
        <activity android:name=".Group.GroupManageActivity"/>
        <activity android:name=".Schedule.AddSchedule"/>
        <activity android:name=".Schedule.SchedulePopUp"/>
    </application>

</manifest>